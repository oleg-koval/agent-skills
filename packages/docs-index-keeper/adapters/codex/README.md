# Codex adapter

Use the canonical skill directly from `packages/docs-index-keeper/SKILL.md`.

Optional repo-local guidance can live in `AGENTS.md` for repositories that need stronger operating instructions.
